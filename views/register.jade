doctype html
head
  title Register
  meta(name='viewport' content='width=device-width, initial-scale=1')
  link(rel='stylesheet' href='/stylesheets/style.css')
  center
    h1#title Register
    p#description
      | <b>#{sender}</b> has invited you to Alsuti.
    form#registerForm.vpad(method='post' action='/register')
      input(name='user' placeholder='User Name')#userEntry
      input(type='password' name='password' placeholder='Password' autocomplete='new-password')#passwordEntry
      input(type='hidden' name='code' value='#{code}')
      button(type='submit') Register
